import { WorkstreamGridView } from '@/components/workstreams/WorkstreamGridView';

export default function AllWorkstreamsPage() {
    return (
        <WorkstreamGridView
            title="All Workstreams"
            description="A comprehensive view of every active, planned, and completed initiative across all tenants."
        />
    );
}
