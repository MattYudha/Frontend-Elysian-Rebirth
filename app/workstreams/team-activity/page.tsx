import { TeamActivityFeed } from '@/components/workstreams/TeamActivityFeed';

export default function TeamActivityPage() {
    return (
        <div className="w-full animate-in fade-in slide-in-from-bottom-6 duration-700">
            <TeamActivityFeed />
        </div>
    );
}
